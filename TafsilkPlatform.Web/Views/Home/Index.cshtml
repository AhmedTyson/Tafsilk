@{
 ViewData["Title"] = "تفصيلك - أمهر الخياطين في منطقتك";
}

@section Styles {
    <style>
        /* Hero Section with Video Background */
     .hero {
     position: relative;
        min-height: 70vh;
  display: flex;
   align-items: center;
        overflow: hidden;
        }

        .hero-video {
    position: absolute;
          inset: 0;
    width: 100%;
 height: 100%;
          object-fit: cover;
   object-position: center;
      z-index: 0;
   background: #0f172a;
         opacity: 0;
   transition: opacity 0.6s ease;
      }

   .hero-video.loaded {
  opacity: 1;
        }

        .hero-video-fallback {
        position: absolute;
            inset: 0;
          display: flex;
 align-items: center;
        justify-content: center;
 padding: 24px;
   text-align: center;
background: radial-gradient(circle at 40% 50%, #1e293b 0%, #0f172a 70%);
   color: rgba(255, 255, 255, 0.85);
   font-size: clamp(18px, 3vw, 34px);
        font-weight: 700;
 line-height: 1.4;
   z-index: 0;
 }

        .hero-overlay {
position: absolute;
     inset: 0;
   background: linear-gradient(135deg, rgba(15, 23, 42, 0.78), rgba(30, 41, 59, 0.82));
   z-index: 1;
     pointer-events: none;
        }

    .hero-content {
   position: relative;
   z-index: 2;
        }

    .hero-title {
        color: var(--text-light);
 font-size: clamp(2rem, 5vw, 3rem);
            margin-bottom: var(--spacing-lg);
  }

    .hero-subtitle {
  color: rgba(255, 255, 255, 0.9);
       font-size: clamp(1rem, 2vw, 1.25rem);
         margin-bottom: var(--spacing-2xl);
  }

        /* Search Widget */
 .search-center {
       margin-top: var(--spacing-2xl);
      }

   .search-widget {
   background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(10px);
    border-radius: var(--radius-xl);
padding: 20px;
     box-shadow: var(--shadow-lg);
   max-width: 600px;
            margin: 0 auto;
        }

 .search-input-group {
       display: flex;
          gap: 8px;
    margin-bottom: 16px;
   }

      .search-input {
       flex: 1;
 padding: 12px 16px;
     border: 2px solid var(--border-color);
  border-radius: var(--radius-lg);
 font-size: 16px;
    font-family: var(--font-family);
        }

     .search-input:focus {
        outline: none;
     border-color: var(--primary-color);
}

    .search-options {
 display: flex;
      gap: 12px;
    justify-content: center;
 flex-wrap: wrap;
         font-size: 14px;
    }

        .search-chip {
    color: rgba(15, 23, 42, 0.7);
       display: flex;
      align-items: center;
      gap: 6px;
        padding: 6px 12px;
     background: var(--background-gray);
 border: 1px solid var(--border-color);
      border-radius: 999px;
            font-size: 13px;
        }

  /* Trust Bar */
        .trust-bar {
    background-color: var(--dark);
      color: white;
         text-align: center;
       padding: 24px 0;
        }

        .trust-indicators {
      display: flex;
            justify-content: center;
      gap: 32px;
         flex-wrap: wrap;
        }

        .trust-item {
     display: flex;
     flex-direction: column;
    align-items: center;
      gap: 4px;
     }

     .trust-number {
      font-size: var(--font-size-2xl);
      font-weight: 700;
            color: var(--text-white);
        }

        .trust-label {
  font-size: var(--font-size-sm);
            color: var(--text-light);
 }

   /* Section Styling */
        .value-props {
         padding: var(--spacing-4xl) 0;
  }

        .section-title {
        text-align: center;
          font-size: var(--font-size-3xl);
font-weight: 700;
          margin-bottom: var(--spacing-2xl);
    }

    .props-grid {
   display: grid;
      grid-template-columns: 1fr;
       gap: var(--spacing-2xl);
}

        @@media (min-width: 768px) {
        .props-grid {
    grid-template-columns: repeat(3, 1fr);
    }
        }

    .prop-card {
   padding: var(--spacing-xl);
    border-radius: var(--radius-lg);
    background-color: var(--background-white);
    box-shadow: var(--shadow-md);
 transition: transform var(--transition-normal);
        display: flex;
 align-items: flex-start;
  gap: 16px;
        }

   .prop-card:hover {
    transform: translateY(-4px);
 box-shadow: var(--shadow-lg);
     }

        .prop-icon {
      width: 48px;
        height: 48px;
   display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 12px;
    font-size: 20px;
         flex-shrink: 0;
      }

   .prop-icon.price {
 background: #e3f2fd;
     color: var(--primary-color);
    }

      .prop-icon.quality {
        background: #e8f5e9;
   color: var(--success-color);
      }

   .prop-icon.location {
      background: #fff3e0;
     color: var(--secondary-color);
     }

.prop-content {
   text-align: right;
        }

        .prop-title {
       font-size: var(--font-size-xl);
            font-weight: 700;
        margin-bottom: var(--spacing-sm);
     }

        .prop-desc {
   color: var(--text-secondary);
  margin: 0;
        }

        /* How It Works */
    .how-it-works {
         background-color: var(--background-gray);
     padding: var(--spacing-4xl) 0;
        }

    .section-subtitle {
    color: var(--text-secondary);
    font-size: 18px;
     text-align: center;
    margin-bottom: var(--spacing-2xl);
        }

 .steps-grid {
   display: grid;
        grid-template-columns: 1fr;
  gap: var(--spacing-2xl);
        }

        @@media (min-width: 768px) {
         .steps-grid {
  grid-template-columns: repeat(3, 1fr);
    }
  }

      .step-card {
     background: var(--background-white);
          border: 1px solid var(--border-color);
   border-radius: var(--radius-lg);
    padding: 24px;
   text-align: center;
 }

    .step-number {
    width: 60px;
       height: 60px;
     background-color: var(--primary-color);
            color: var(--text-white);
   border-radius: 50%;
  display: flex;
  align-items: center;
            justify-content: center;
         font-size: var(--font-size-xl);
font-weight: 700;
  margin: 0 auto var(--spacing-lg);
 }

      .step-title {
 font-size: var(--font-size-xl);
  font-weight: 700;
 margin-bottom: var(--spacing-md);
        }

        .step-desc {
            color: var(--text-secondary);
     margin: 0;
        }

    /* Featured Tailors */
    .featured-tailors {
          padding: var(--spacing-4xl) 0;
        }

        .tailors-carousel {
            display: flex;
            gap: 20px;
   overflow-x: auto;
   padding: 8px 0 16px;
   scrollbar-width: thin;
 }

        .tailor-card {
 min-width: 280px;
            background: var(--background-white);
       border: 1px solid var(--border-color);
    border-radius: var(--radius-lg);
      overflow: hidden;
   }

    .tailor-image {
  width: 100%;
         height: 160px;
         object-fit: cover;
 background: linear-gradient(135deg, #f1f5f9, #e2e8f0);
        }

.tailor-info {
     padding: var(--spacing-lg);
    }

     .tailor-name-row {
     display: flex;
   align-items: center;
   gap: var(--spacing-sm);
   margin-bottom: var(--spacing-sm);
      }

        .tailor-name {
    font-size: var(--font-size-lg);
     font-weight: 700;
  margin: 0;
     flex: 1;
}

        .verified-badge {
     background-color: var(--success-color);
         color: var(--text-white);
     padding: 4px 8px;
   border-radius: var(--radius-sm);
  font-size: var(--font-size-xs);
    font-weight: 600;
 display: flex;
   align-items: center;
     gap: 4px;
   }

  .tailor-rating {
            display: flex;
       align-items: center;
      gap: var(--spacing-sm);
    margin-bottom: var(--spacing-sm);
        }

     .rating {
     color: var(--secondary-color);
    font-weight: 600;
        }

        .reviews {
      color: var(--text-secondary);
    font-size: var(--font-size-sm);
        }

 /* Service Categories */
        .service-categories {
background-color: var(--background-gray);
 padding: var(--spacing-4xl) 0;
        }

        .categories-grid {
        display: grid;
      gap: 16px;
      grid-template-columns: repeat(2, 1fr);
        }

  @@media (min-width: 768px) {
  .categories-grid {
     grid-template-columns: repeat(5, 1fr);
  }
      }

        .category-card {
      background-color: var(--background-white);
 padding: 20px;
  border-radius: var(--radius-lg);
 text-align: center;
    box-shadow: var(--shadow-sm);
   transition: all var(--transition-normal);
      cursor: pointer;
 }

    .category-card:hover {
       background: var(--background-light);
         transform: translateY(-2px);
        box-shadow: var(--shadow-md);
        }

        .category-icon {
            width: 48px;
    height: 48px;
     margin: 0 auto var(--spacing-md);
     display: flex;
 align-items: center;
          justify-content: center;
 background: var(--background-light);
       border-radius: var(--radius-md);
  font-size: 24px;
     }

      .category-title {
    font-size: var(--font-size-base);
  font-weight: 600;
            margin-bottom: 4px;
        }

   .service-description {
         color: var(--text-secondary);
        font-size: 13px;
        }

    /* Tailor Recruitment */
        .tailor-recruitment {
  padding: var(--spacing-4xl) 0;
 }

    .recruitment-content {
       background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
     color: white;
        border-radius: var(--radius-lg);
        padding: 32px;
       text-align: center;
      }

        .recruitment-content .section-title {
        color: white;
        max-width: 800px;
       margin: 0 auto var(--spacing-2xl);
     }

     .join-benefits {
     display: flex;
 gap: 12px;
  flex-wrap: wrap;
      justify-content: center;
            margin-bottom: 20px;
   }

        .benefit-pill {
  background: rgba(255, 255, 255, 0.2);
    color: white;
      padding: 8px 16px;
       border-radius: 999px;
        font-size: 14px;
        font-weight: 600;
   display: flex;
        align-items: center;
        gap: 8px;
        }
    </style>
}

<main>
    <!-- Hero Section with Video Background -->
    <section id="home" class="hero">
        <video class="hero-video" id="heroVideo" autoplay muted loop playsinline preload="auto" aria-label="خلفية توضيحية لورش وخدمات الخياطة">
   <source src="~/video.mp4" type="video/mp4" />
        </video>
        <div class="hero-video-fallback" id="heroVideoFallback" style="display: none">
    <div class="fallback-content">
 <i class="fas fa-cut fa-3x"></i>
  <h2>تفصيلك – اكتشف أمهر الخياطين بسهولة</h2>
       <p>منصة رقمية تربطك بأمهر الخياطين في منطقتك</p>
      </div>
     </div>
   <div class="hero-overlay" id="heroOverlay"></div>

 <div class="container">
 <div class="hero-content">
       <h1 class="hero-title">أمهر الخياطين في منطقتك. موثوقين وبالأسعار</h1>
          <p class="hero-subtitle">تصفح أعمالهم، اعرف الأسعار، واحجز معادك في دقائق</p>

    <!-- Search Center -->
     <div class="search-center">
    <div class="search-widget">
<form class="search-form" asp-controller="Home" asp-action="SearchTailors" method="get">
     <div class="search-input-group">
    <input type="text" name="query" class="search-input" placeholder="ابحث عن خياط قريب منك..." aria-label="البحث عن خياط" />
          <button type="submit" class="btn btn-primary">
    <i class="fas fa-search"></i>
         <span>ابحث</span>
            </button>
      </div>

     <div class="search-options">
                <label class="search-chip">
   <input type="checkbox" name="express" />
     <i class="fas fa-clock"></i>
     <span>خدمة عاجلة (اليوم/غدًا)</span>
   </label>
     <div class="search-chip">
  <i class="fas fa-map-marker-alt"></i>
     <span>نطاق: 5 كم</span>
    </div>
  </div>
      </form>
      </div>
    </div>
         </div>
   </div>
    </section>

    <!-- Trust Indicators -->
    <section class="trust-bar">
 <div class="container">
     <div class="trust-indicators">
       <div class="trust-item">
    <div class="trust-dot"><i class="fas fa-users"></i></div>
       <span class="trust-number">300+</span>
      <span class="trust-label">خياط موثق</span>
    </div>
 <div class="trust-item">
    <div class="trust-dot"><i class="fas fa-check-circle"></i></div>
  <span class="trust-number">5,000+</span>
    <span class="trust-label">طلب مكتمل</span>
    </div>
           <div class="trust-item">
     <div class="trust-dot"><i class="fas fa-map-marked-alt"></i></div>
           <span class="trust-number">3 مدن</span>
   <span class="trust-label">نخدم القاهرة، الجيزة، والإسكندرية</span>
   </div>
  </div>
        </div>
    </section>

    <!-- Value Propositions -->
    <section class="value-props">
        <div class="container">
   <h2 class="section-title">لماذا تفصيلك؟</h2>
    <div class="props-grid">
  <div class="prop-card">
           <div class="prop-icon price">
   <i class="fas fa-money-bill-wave fa-2x"></i>
  </div>
<div class="prop-content">
     <h3 class="prop-title">أسعار شفافة</h3>
      <p class="prop-desc">اعرف تكلفة الخدمة قبل ما تحجز</p>
            </div>
     </div>

   <div class="prop-card">
        <div class="prop-icon quality">
    <i class="fas fa-shield-alt fa-2x"></i>
          </div>
  <div class="prop-content">
     <h3 class="prop-title">جودة موثوقة</h3>
          <p class="prop-desc">شوف صور شغلهم وتقييمات عملاء حقيقيين</p>
       </div>
   </div>

  <div class="prop-card">
         <div class="prop-icon location">
    <i class="fas fa-map-marker-alt fa-2x"></i>
 </div>
   <div class="prop-content">
         <h3 class="prop-title">خياطين في كل مكان</h3>
             <p class="prop-desc">اكتشف أمهر الخياطين في منطقتك بسهولة</p>
        </div>
  </div>
        </div>
     </div>
    </section>

    <!-- How It Works -->
    <section id="how-it-works" class="how-it-works">
<div class="container">
     <h2 class="section-title">كيف تعمل المنصة؟</h2>
          <p class="section-subtitle">خطوات بسيطة للحصول على أفضل خدمات الخياطة</p>
   <div class="steps-grid">
   <div class="step-card">
<div class="step-number">1</div>
     <h3 class="step-title">ابحث</h3>
          <p class="step-desc">حدد منطقتك ونوع الخدمة اللي محتاجها</p>
    </div>

          <div class="step-card">
      <div class="step-number">2</div>
       <h3 class="step-title">قارن واحجز</h3>
   <p class="step-desc">شوف صور الشغل، الأسعار، والتقييمات، واحجز معادك</p>
         </div>

   <div class="step-card">
         <div class="step-number">3</div>
    <h3 class="step-title">استلم</h3>
   <p class="step-desc">تابع طلبك واستلم في الميعاد المتفق عليه</p>
     </div>
     </div>
        </div>
  </section>

    <!-- Featured Tailors -->
    <section id="tailors" class="featured-tailors">
    <div class="container">
            <h2 class="section-title">تعرف على أمهر الخياطين في منطقتك</h2>
       <div class="carousel-container">
     <div class="tailors-carousel">
<div class="tailor-card">
       <img src="https://placehold.co/400x300/e3f2fd/2c5aa0?text=خياط+محترف" alt="خياط محترف" class="tailor-image" />
    <div class="tailor-info">
    <div class="tailor-name-row">
    <h3 class="tailor-name">أستاذ محمد أحمد</h3>
   <span class="verified-badge">
    <i class="fas fa-check-circle"></i>
    <span>موثق</span>
     </span>
  </div>
    <div class="tailor-rating">
   <span class="rating"><i class="fas fa-star"></i> 4.8</span>
 <span class="reviews">(120 تقييم)</span>
   </div>
           <p class="tailor-specialty">
   <i class="fas fa-tshirt"></i> فساتين سهرة
    </p>
   <p class="tailor-location">
  <i class="fas fa-map-marker-alt"></i> مصر الجديدة
    </p>
      <a href="#" class="btn btn-primary">عرض الملف الشخصي</a>
     </div>
     </div>

     <div class="tailor-card">
      <img src="https://placehold.co/400x300/e3f2fd/2c5aa0?text=خياطة+محترفة" alt="خياط محترف" class="tailor-image" />
     <div class="tailor-info">
   <div class="tailor-name-row">
      <h3 class="tailor-name">أستاذة فاطمة علي</h3>
      <span class="verified-badge">
       <i class="fas fa-check-circle"></i>
  <span>موثق</span>
   </span>
         </div>
        <div class="tailor-rating">
       <span class="rating"><i class="fas fa-star"></i> 4.9</span>
          <span class="reviews">(89 تقييم)</span>
        </div>
     <p class="tailor-specialty">
        <i class="fas fa-user-tie"></i> بدل رجالي
      </p>
<p class="tailor-location">
<i class="fas fa-map-marker-alt"></i> المعادي
    </p>
         <a href="#" class="btn btn-primary">عرض الملف الشخصي</a>
            </div>
    </div>

       <div class="tailor-card">
       <img src="https://placehold.co/400x300/e3f2fd/2c5aa0?text=خياط+محترف" alt="خياط محترف" class="tailor-image" />
    <div class="tailor-info">
              <div class="tailor-name-row">
      <h3 class="tailor-name">أستاذ أحمد حسن</h3>
     <span class="verified-badge">
         <i class="fas fa-check-circle"></i>
        <span>موثق</span>
        </span>
       </div>
      <div class="tailor-rating">
   <span class="rating"><i class="fas fa-star"></i> 4.9</span>
             <span class="reviews">(156 تقييم)</span>
  </div>
     <p class="tailor-specialty">
    <i class="fas fa-tools"></i> تعديل ملابس
      </p>
        <p class="tailor-location">
        <i class="fas fa-map-marker-alt"></i> الزمالك
       </p>
       <a href="#" class="btn btn-primary">عرض الملف الشخصي</a>
</div>
    </div>
     </div>
   </div>
        </div>
    </section>

    <!-- Service Categories -->
    <section class="service-categories">
      <div class="container">
       <h2 class="section-title">الخدمات الشائعة</h2>
<p class="section-subtitle">اختر من بين خدماتنا المتعددة التي نقدمها بأعلى جودة وأفضل الأسعار</p>
        <div class="categories-grid">
  <div class="category-card">
 <div class="category-icon">
       <i class="fas fa-tools fa-2x"></i>
         </div>
     <h3 class="category-title">تعديل ملابس</h3>
          <p class="service-description">تصليح جميع أنواع الملابس</p>
 </div>

     <div class="category-card">
             <div class="category-icon">
            <i class="fas fa-tshirt fa-2x"></i>
         </div>
    <h3 class="category-title">تفصيل فساتين</h3>
          <p class="service-description">تعديل وتفصيل فساتين السهرة</p>
            </div>

  <div class="category-card">
      <div class="category-icon">
   <i class="fas fa-user-tie fa-2x"></i>
        </div>
     <h3 class="category-title">بدل رجالي</h3>
  <p class="service-description">تفصيل بدلة بكافة المقاسات</p>
   </div>

   <div class="category-card">
 <div class="category-icon">
     <i class="fas fa-child fa-2x"></i>
 </div>
           <h3 class="category-title">ملابس أطفال</h3>
 <p class="service-description">تفصيل ملابس الأطفال</p>
      </div>

   <div class="category-card">
 <div class="category-icon">
       <i class="fas fa-wrench fa-2x"></i>
    </div>
  <h3 class="category-title">تصليح</h3>
   <p class="service-description">إصلاح وصيانة الملابس</p>
    </div>
    </div>
        </div>
    </section>

    <!-- Tailor Recruitment -->
    <section class="tailor-recruitment">
        <div class="container">
       <div class="recruitment-content">
       <h2 class="section-title">انضم لشبكة تفصيلك. كّبر شغلك ووصل لعملاء أكتر</h2>
        <div class="join-benefits">
 <span class="benefit-pill">
   <i class="fas fa-users"></i>
   <span>+50% عملاء أكثر</span>
      </span>
   <span class="benefit-pill">
   <i class="fas fa-calendar-alt"></i>
      <span>إدارة سهلة للمواعيد</span>
         </span>
     <span class="benefit-pill">
       <i class="fas fa-portrait"></i>
    <span>ملف أعمال احترافي</span>
   </span>
      </div>
<div class="recruitment-cta">
          <a asp-controller="Account" asp-action="Register" class="btn btn-dark btn-large">
             <i class="fas fa-plus-circle"></i>
   <span>سجل ورشتك الآن</span>
   </a>
        </div>
      </div>
   </div>
    </section>
</main>

@section Scripts {
<script>
      // Video handler
      document.addEventListener("DOMContentLoaded", function () {
  const video = document.getElementById('heroVideo');
 const fallback = document.getElementById('heroVideoFallback');

    if (video) {
   video.addEventListener('loadeddata', function () {
      video.classList.add('loaded');
        });

 video.addEventListener('error', function () {
             video.style.display = 'none';
   fallback.style.display = 'flex';
           });

      // Show fallback if video doesn't load within 3 seconds
           setTimeout(function () {
 if (!video.classList.contains('loaded')) {
      video.style.display = 'none';
          fallback.style.display = 'flex';
    }
    }, 3000);
         }
      });
    </script>
}
