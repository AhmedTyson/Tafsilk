# 🎨 Account Controller Visual Flow Diagrams

## 📊 Complete Navigation Flow Map

```
┌─────────────────────────────────────────────────────────────────────────────┐
│          ACCOUNT CONTROLLER NAVIGATION MAP          │
└─────────────────────────────────────────────────────────────────────────────┘

      ┌─────────────┐
            │  Home Page  │
              │ /Home/Index │
       └──────┬──────┘
       │
        ┌────────────────┼────────────────┐
            │    │  │
       ↓       ↓                ↓
   ┌─────────────────┐  ┌────────────┐  ┌─────────────────┐
       │ Login (Browse)  │  │ OAuth Flow │  │ Direct Register │
   │ /Account/Login  │  │            │  │/Account/Register│
   └────────┬────────┘  └─────┬──────┘  └────────┬────────┘
    │        │       │
    │        │                 │
[See FLOW 1]    │     [See FLOW 2] │      [See FLOW 3]│
                ↓       ↓           ↓
```

---

## 🔵 FLOW 1: Standard Login Flow

```
┌──────────────────────────────────────────────────────────────────────────┐
│  STANDARD LOGIN FLOW      │
└──────────────────────────────────────────────────────────────────────────┘

User arrives at:
┌─────────────────┐
│ GET /Account/   │
│   Login      │
│    │
│ View: Login.    │
│   cshtml    │
└────────┬────────┘
       │
       │ User fills form:
       │ • Email
         │ • Password
         │ • Remember Me (checkbox)
 │
       │ Clicks "تسجيل الدخول"
       │
         ↓
┌─────────────────┐
│ POST /Account/  │
│    Login    │
│       │
│ AccountController│
│ .Login()        │
└────────┬────────┘
         │
         │ Processing...
         │ 1. Input sanitization ✓
      │ 2. Rate limit check ✓
         │ 3. Credential validation ✓
       │ 4. Account status check ✓
         │
         ├──────────────┬─────────────┬──────────────┐
         │         │       │   │
    ✅ Success    ❌ Failed    ⚠️ Special   🔒 Locked
         │              │           │     │
         ↓      ↓        ↓  ↓
┌────────────────┐ ┌─────────┐ ┌──────────┐ ┌─────────────┐
│Sign In Success │ │ Stay on │ │  Tailor  │ │   Return    │
│Build Claims    │ │ Login   │ │ Incomplete│ │  to Login   │
│Create Session  │ │ View    │ │  Profile │ │   + Error   │
└────────┬───────┘ │ + Error │ └────┬─────┘ │"Rate limited"│
 │         └─────────┘      │   └─────────────┘
         │       │
     ↓        ↓
┌─────────────────┐       ┌──────────────────┐
│ Redirect by     │   │Sign in temporarily│
│ Role:           │       │then redirect:│
│          │       │         │
│ • Tailor    ────┼──→    │/Account/Complete  │
│ → /Dashboards/│     │TailorProfile      │
│     Tailor      ││?incomplete=true   │
│                 │       └──────────────────┘
│ • Corporate ────┼──→
│   → /Dashboards/│
│     Corporate   │
│                 │
│ • Customer  ────┼──→
│   → /Dashboards/│
│     Customer    │
│         │
│ OR         │
│ • returnUrl ────┼──→ Custom URL
└─────────────────┘
```

---

## 🟢 FLOW 2: OAuth Login Flow (Google/Facebook)

```
┌──────────────────────────────────────────────────────────────────────────┐
│      OAUTH LOGIN/REGISTRATION FLOW  │
└──────────────────────────────────────────────────────────────────────────┘

User on Login Page:
┌─────────────────┐
│ Login.cshtml    │
│       │
│ Clicks Google/  │
│ Facebook button │
└────────┬────────┘
     │
         ├────────────────┬────────────────┐
   │ │           │
 Google 🔴    Facebook 🔵      (Same flow)
         │              │
         ↓     ↓
┌─────────────────┐ ┌─────────────────┐
│GET /Account/    │ │GET /Account/    │
│  GoogleLogin  │ │  FacebookLogin  │
└────────┬────────┘ └────────┬────────┘
      │        │
         │ Challenge for     │ Challenge for
         │ "Google" scheme   │ "Facebook" scheme
         │    │
    ↓            ↓
┌─────────────────────────────────────┐
│  External OAuth Provider            │
│  (Google/Facebook Login Page)       │
│    │
│  User authenticates...     │
└──────────────┬──────────────────────┘
      │
         │ Callback with auth result
         │
         ┌─────┴─────┐
 │           │
         ↓           ↓
┌──────────────┐ ┌────────────────┐
│GET /Account/ │ │GET /Account/   │
│GoogleResponse│ │FacebookResponse│
└──────┬───────┘ └──────┬─────────┘
       │         │
       └────────┬───────┘
      │
         │ Extract OAuth claims:
  │ • Email
                │ • Name
    │ • Picture
      │ • Provider ID
             │
             ↓
        ┌───────────────┐
  │ Check if user │
   │ exists by     │
  │ email │
        └───────┬───────┘
   │
        ┌───────┴───────┐
      ││
     EXISTS         NEW USER
        │            │
   ↓               ↓
┌──────────────┐  ┌─────────────────────┐
│Sign in       │  │Store in TempData:   │
│existing user │  │• OAuthProvider      │
│              │  │• OAuthEmail  │
│Redirect by   │  │• OAuthName  │
│role          │  │• OAuthPicture│
└──────────────┘  │• OAuthId  │
    └──────────┬──────────┘
    │
              ↓
        ┌──────────────────────┐
          │GET /Account/         │
      │CompleteSocial │
 │Registration          │
            │               │
        │View: CompleteGoogle  │
         │Registration.cshtml   │
   └──────────┬───────────┘
      │
      │ User fills:
             │ • Full Name (pre-filled)
 │ • Phone Number
 │ • User Type:
     │   ○ Customer
        │   ○ Tailor
   │   ○ Corporate
        │
     ↓
           ┌──────────────────────┐
             │POST /Account/        │
      │CompleteSocial        │
  │Registration        │
         └──────────┬───────────┘
          │
      │ Create user account
     │ (OAuth password)
        │
     ┌────────┴────────┐
  │          │
              CUSTOMER/           TAILOR
   CORPORATE
         │       │
      ↓      ↓
 ┌──────────────┐   ┌─────────────────┐
       │Auto sign in  │   │Redirect to: │
          ││   │/Account/Complete│
     │Redirect by   │   │TailorProfile    │
  │role          │   │            │
          └──────────────┘   │[See FLOW 4]     │
           └─────────────────┘
```

---

## 🟡 FLOW 3: Standard Registration Flow

```
┌──────────────────────────────────────────────────────────────────────────┐
│  STANDARD REGISTRATION FLOW       │
└──────────────────────────────────────────────────────────────────────────┘

User arrives at:
┌─────────────────┐
│ GET /Account/   │
│    Register   │
│      │
│ View: Register. │
│       cshtml    │
└────────┬────────┘
         │
         │ User fills form:
         │ • Name
         │ • Email
         │ • Password
         │ • Phone Number (optional)
         │ • User Type:
         │   ○ Customer (default)
         │   ○ Tailor
         │○ Corporate
   │
     │ Clicks "اشترك الآن"
  │
         ↓
┌─────────────────┐
│ POST /Account/  │
│    Register │
│     │
│ AccountController│
│ .Register()     │
└────────┬────────┘
         │
  │ Processing:
   │ 1. Input sanitization ✓
    │ 2. Check email exists ✓
         │ 3. Check phone exists ✓
         │ 4. Validate password ✓
         │ 5. Create user account ✓
         │
    ┌────┴────┬──────────┐
    │         │        │
CUSTOMER  CORPORATE  TAILOR
    │         │        │
    ↓         ↓       ↓
┌─────────┐ ┌─────────┐ ┌──────────────────┐
│Create   │ │Create   │ │Create User     │
│Customer │ │Corporate│ │IsActive = FALSE  │
│Profile  │ │Account  │ │NO PROFILE YET!   │
│         │ ││ │   │
│Send     │ │Send     │ │Call:             │
│email    │ │email    │ │RedirectToTailor  │
│verify   │ │verify   │ │EvidenceSubmission│
└────┬────┘ └────┬────┘ └────────┬─────────┘
   │           │       │
     │        │               │ Store in TempData:
     │        │           │ • TailorUserId
     │           │       │ • TailorEmail
  │           │    │ • TailorName
     │         │          │
     ↓     ↓       ↓
┌────────────────────┐  ┌─────────────────┐
│TempData["Success"] │  │Redirect to:     │
│= "تم إنشاء الحساب"│  │/Account/Complete│
│           │  │TailorProfile    │
│Redirect to:      │  │       │
│/Account/Login      │  │[See FLOW 4]     │
└────────────────────┘  └─────────────────┘
```

---

## 🔴 FLOW 4: Tailor Evidence Submission (MANDATORY)

```
┌──────────────────────────────────────────────────────────────────────────┐
│      TAILOR EVIDENCE SUBMISSION FLOW           │
│     (ONE-TIME MANDATORY)   │
└──────────────────────────────────────────────────────────────────────────┘

Entry Points:
┌────────────────┬──────────────────┬──────────────────────┐
│ After Tailor   │ After OAuth      │ Middleware Redirect  │
│ Registration   │ Tailor Complete  │ (Incomplete Login)   │
└────────┬───────┴────────┬─────────┴──────────┬───────────┘
    │ │        │
      └────────────────┼────────────────────┘
        │
      ↓
     ┌──────────────────────┐
       │GET /Account/         │
                │CompleteTailorProfile │
         │     │
      │View: CompleteTailor  │
           │Profile.cshtml        │
└──────────┬───────────┘
        │
       │ Check if authenticated:
│
        ┌────────────────┼────────────────┐
          │          │  │
    AUTHENTICATED   NEW REGISTRATION   NO SESSION
    (has User.Id)   (has TempData)(neither)
    │      │      │
          ↓     ↓       ↓
┌────────────────┐ ┌──────────────┐ ┌──────────────┐
│Check profile   │ │Check profile │ │TempData[""]= │
│exists          │ │exists        │ │"جلسة غير صالحة"│
│             │ │     │ │      │
│If exists:    │ │If exists:    │ │Redirect to:  │
│→ Dashboard     │ │→ Login  │ │/Account/Login│
│       │ │         │ └──────────────┘
│If not exists:  │ │If not exists:│
│→ Show form     │ │→ Show form   │
└────────┬───────┘ └──────┬───────┘
         │            │
       └────────┬───────┘
              │
              │ User fills MANDATORY form:
        │ ✓ Workshop Name
        │ ✓ Workshop Type
    │ ✓ Phone Number
         │ ✓ Address
     │ ✓ City
             │ ✓ Description
   │ ✓ Experience Years
          │ ✓ ID Document (IMAGE)
      │ ✓ Portfolio Images (3+ IMAGES)
            │ ✓ Agree to Terms
        │
        ↓
        ┌──────────────────────┐
  │POST /Account/        │
  │CompleteTailorProfile │
      └──────────┬───────────┘
   │
      │ Validation:
       │ 1. Check duplicate submission ✓
  │ 2. Verify user is tailor ✓
                   │ 3. Validate ID document ✓
       │ 4. Validate portfolio (min 3) ✓
        │ 5. Check file types/sizes ✓
    │
        ┌──────────┴──────────┐
      │              │
    ✅ VALID        ❌ INVALID
        │       │
        ↓      ↓
┌───────────────────┐  ┌─────────────┐
│1. Create Tailor   │  │Return to │
│   Profile      │  │form with    │
│          │  │validation   │
│2. Save ID to DB   │  │errors       │
│   (binary)        │  └─────────────┘
│       │
│3. Save portfolio  │
│   to disk       │
│   (/uploads/    │
│    portfolio/)    │
│      │
│4. Set User        │
│   IsActive = true │
│               │
│5. Send email   │
│   verification    │
│        │
│6. TempData:     │
│   "تم إكمال التسجيل│
│   بنجاح..."       │
└──────────┬────────┘
           │
       ↓
    ┌──────────────┐
    │Redirect to:  │
    │/Account/Login│
    └──────┬───────┘
           │
           │ User can now login!
           │ (Profile exists ✓)
        │ (IsActive = true ✓)
        │
 ↓
    [Back to FLOW 1]
```

---

## 🟣 FLOW 5: Password Management

```
┌──────────────────────────────────────────────────────────────────────────┐
│       PASSWORD MANAGEMENT FLOW  │
└──────────────────────────────────────────────────────────────────────────┘

User (authenticated) clicks:
"Change Password" in settings
   │
         ↓
┌─────────────────┐
│GET /Account/    │
│ChangePassword   │
│      │
│View: Change     │
│Password.cshtml  │
└────────┬────────┘
 │
         │ User fills:
     │ • Current Password
         │ • New Password
         │ • Confirm Password
         │
         │ (Live validation shows:)
         │ • 8+ characters
       │ • Uppercase letter
         │ • Number
    │ • Special character
         │
         ↓
┌─────────────────┐
│POST /Account/   │
│ChangePassword   │
└────────┬────────┘
         │
         │ Validation:
      │ 1. Verify current password ✓
         │ 2. Check new password rules ✓
         │ 3. Confirm passwords match ✓
         │
    ┌────┴────┐
    │    │
 ✅ VALID ❌ INVALID
    ││
    ↓         ↓
┌─────────┐ ┌──────────┐
│Update   │ │Return to │
│password │ │form with │
│hash     │ │errors    │
│         │ └──────────┘
│TempData:│
│"تم تغيير│
│كلمة المرور"│
└────┬────┘
     │
     ↓
┌──────────────┐
│Redirect to   │
│dashboard     │
│(by role)     │
└──────────────┘
```

---

## 🟠 FLOW 6: Role Change (Customer → Tailor)

```
┌──────────────────────────────────────────────────────────────────────────┐
│   ROLE CHANGE FLOW (Customer → Tailor)     │
└──────────────────────────────────────────────────────────────────────────┘

User (Customer) wants to become Tailor:
         │
         ↓
┌─────────────────┐
│GET /Account/    │
│RequestRoleChange│
│              │
│View: Request    │
│RoleChange.cshtml│
└────────┬────────┘
         │
         │ User fills:
 │ • Shop Name
         │ • Address
 │ • Experience Years
         │ • Reason (20-500 chars)
         │ • Business License (optional image)
    │
         ↓
┌─────────────────┐
│POST /Account/│
│RequestRoleChange│
└────────┬────────┘
 │
       │ Validation:
         │ 1. Check current role ✓
         │ 2. Validate shop info ✓
         │ 3. Check reason length ✓
         │
    ┌────┴────┐
    │         │
 ✅ VALID ❌ INVALID
    │         │
    ↓         ↓
┌──────────┐ ┌──────────┐
│1. Get    │ │Return to │
│ Tailor │ │form with │
│   Role   │ │errors    │
│      │ └──────────┘
│2. Update │
│   User   │
│   RoleId │
│          │
│3. Create │
│   Tailor │
│   Profile│
│          │
│4. Sign   │
│   Out    │
│   User   │
│    │
│5. TempData│
│   Success│
└────┬─────┘
  │
     ↓
┌──────────────┐
│Redirect to:  │
│/Account/Login│
│       │
│User must     │
│re-login with │
│new role      │
└──────────────┘
```

---

## 🔵 FLOW 7: Email Verification

```
┌──────────────────────────────────────────────────────────────────────────┐
│        EMAIL VERIFICATION FLOW         │
└──────────────────────────────────────────────────────────────────────────┘

After Registration:
┌─────────────────┐
│Email sent with  │
│verification   │
│token  │
└────────┬────────┘
         │
       │ User clicks link:
   │ /Account/VerifyEmail
       │ ?token={32-char-token}
         │
         ↓
┌─────────────────┐
│GET /Account/    │
│VerifyEmail      │
│?token=...       │
└────────┬────────┘
         │
         │ Processing:
         │ 1. Find user by token ✓
         │ 2. Check token expiry ✓
     │ 3. Mark email verified ✓
         │
    ┌────┴────┐
    │    │
 ✅ VALID ❌ INVALID/EXPIRED
    │         │
    ↓         ↓
┌──────────┐ ┌───────────┐
│Set:      │ │TempData:  │
│Email     │ │"انتهت صلاحية│
│Verified  │ │الرابط..."  │
│= true    │ │         │
│          │ │Redirect:  │
│Send      │ │→ Login    │
│Welcome   │ └───────────┘
│Email     │
│  │
│TempData: │
│"تم تأكيد │
│بريدك..."  │
└────┬─────┘
     │
     ↓
┌──────────────┐
│Redirect to:  │
│/Account/Login│
└──────────────┘


RESEND FLOW:
User didn't receive email
         │
   ↓
┌─────────────────┐
│GET /Account/    │
│ResendVerification│
│Email            │
│       │
│View: Resend...  │
└────────┬────────┘
       │
  │ User enters email
     │
         ↓
┌─────────────────┐
│POST /Account/   │
│ResendVerification│
│Email  │
└────────┬────────┘
   │
         │ Processing:
         │ 1. Find user by email ✓
   │ 2. Check if verified ✓
         │ 3. Generate new token ✓
     │ 4. Send email ✓
         │
         ↓
┌─────────────────┐
│Stay on same page│
│Show success msg │
│"تم إرسال رسالة   │
│التحقق..."      │
└─────────────────┘
```

---

## 🔄 FLOW 8: Logout

```
┌──────────────────────────────────────────────────────────────────────────┐
│       LOGOUT FLOW     │
└──────────────────────────────────────────────────────────────────────────┘

User (authenticated) clicks:
"Logout" button
         │
   ↓
┌─────────────────┐
│POST /Account/   │
│   Logout   │
└────────┬────────┘
         │
       │ Processing:
         │ 1. Sign out from cookie auth ✓
         │ 2. Clear session ✓
  │
      ↓
┌─────────────────┐
│Redirect to:     │
│/Home/Index      │
│          │
│User is now      │
│anonymous        │
└─────────────────┘
```

---

## 📊 Summary: All Entry/Exit Points

```
┌──────────────────────────────────────────────────────────────────────────┐
│            ALL ACCOUNT CONTROLLER ROUTES        │
└──────────────────────────────────────────────────────────────────────────┘

ENTRY POINTS (Views Users Can Access):
┌─────────────────────────────┬────────────┬──────────────┐
│ URL     │ Auth Req'd │ View File    │
├─────────────────────────────┼────────────┼──────────────┤
│ /Account/Login  │ ❌ No      │ Login.cshtml │
│ /Account/Register         │ ❌ No      │ Register.cshtml │
│ /Account/CompleteTailorProfile│ ⚠️ Mixed │ CompleteTailorProfile.cshtml │
│ /Account/CompleteSocialRegistration│ ❌ No│ CompleteGoogleRegistration.cshtml │
│ /Account/ChangePassword     │ ✅ Yes     │ ChangePassword.cshtml │
│ /Account/RequestRoleChange  │ ✅ Yes     │ RequestRoleChange.cshtml │
│ /Account/ResendVerificationEmail│ ❌ No  │ ResendVerificationEmail.cshtml │
└─────────────────────────────┴────────────┴──────────────┘

EXIT POINTS (Where Users Are Redirected After Success):
┌──────────────────────────────────┬──────────────────────┐
│ From Action      │ Redirect Destination │
├──────────────────────────────────┼──────────────────────┤
│ Login (Customer)       │ /Dashboards/Customer │
│ Login (Tailor)     │ /Dashboards/Tailor   │
│ Login (Corporate)  │ /Dashboards/Corporate│
│ Login (Incomplete Tailor)     │ /Account/CompleteTailorProfile │
│ Register (Customer/Corporate)    │ /Account/Login       │
│ Register (Tailor)       │ /Account/CompleteTailorProfile │
│ CompleteTailorProfile│ /Account/Login       │
│ CompleteSocialRegistration (C/C) │ /Dashboards/...      │
│ CompleteSocialRegistration (T)   │ /Account/CompleteTailorProfile │
│ ChangePassword          │ /Dashboards/...      │
│ RequestRoleChange         │ /Account/Login       │
│ VerifyEmail         │ /Account/Login │
│ Logout│ /Home/Index    │
└──────────────────────────────────┴──────────────────────┘

EXTERNAL REDIRECTS (OAuth):
┌──────────────────────┬─────────────────────────────────┐
│ Action    │ External URL     │
├──────────────────────┼─────────────────────────────────┤
│ GoogleLogin          │ https://accounts.google.com/... │
│ FacebookLogin        │ https://www.facebook.com/...  │
└──────────────────────┴─────────────────────────────────┘

CALLBACKS (From External OAuth):
┌──────────────────────┬─────────────────────────────────┐
│ URL   │ Then Redirects To     │
├──────────────────────┼─────────────────────────────────┤
│ /Account/GoogleResponse │ Dashboard OR CompleteSocialRegistration │
│ /Account/FacebookResponse│ Dashboard OR CompleteSocialRegistration │
└──────────────────────┴─────────────────────────────────┘
```

---

**Document Version**: 1.0  
**Total Flows Mapped**: 8  
**Total Entry Points**: 7 views  
**Total Exit Points**: 10+ destinations  
**Status**: ✅ Complete & Visual
